# Soga Knowledge Graph Platform：專案技術介紹與簡報素材

> 文件目的：說明本專案的產品定位、模組職責、資料流程、AI Agent 呼叫鏈與 API 參數。可直接依章節製作簡報。
>
> 文件依據：目前程式碼實作（2026-09-21）。「已實作」與「預留／未完成」功能會明確區分，避免簡報將規劃功能誤稱為可用能力。

## 1. 一句話定位

Soga 是一個把關聯式資料庫（目前正式支援 SQLite）轉換為 Neo4j 知識圖譜的平台。使用者不必先寫 Cypher，而是透過網頁建立專案、探勘資料表、調整 Entity／Property／Relation Mapping、執行建置，最後以圖譜視覺化、結構化查詢或自然語言 Agent 查詢資料。

核心原則是 **Mapping 驅動，而非產業模型寫死**：資產資料只是目前的示範；任何資料表、實體名稱、欄位與關係都由每個專案的 YAML Mapping 定義。

## 2. 系統全貌

```text
使用者
  │
  ▼
React / Vite 前端
  │  HTTP JSON
  ▼
FastAPI API（/builder、/query）
  ├─ 平台中繼資料：SQLite platform_state.db
  │    專案、資料來源設定、建置工作、Mapping 版本
  ├─ 原始資料來源：SQLite .db（目前唯一正式支援的讀取器）
  ├─ Mapping：semantic/projects/<project_id>/mappings/*.yaml
  ├─ 建置管線：IngestionPipeline
  │    └─ Neo4j（節點、關係、全文索引）
  └─ AI 查詢 Agent：LangGraph + OpenAI-compatible LLM
       └─ 將自然語言轉為 SemanticQuery，再安全地產生 Cypher 查 Neo4j
```

| 層級 | 技術／檔案 | 職責 |
|---|---|---|
| 使用者介面 | React 19、React Router、Vite | 專案管理、資料來源設定、Mapping 編輯、建置、圖譜與對話 |
| Web API | FastAPI，`backend/app/main.py` | 提供 REST API、CORS 與背景工作派送 |
| 平台狀態 | SQLite，`backend/data/platform_state.db` | 保存系統本身的設定與工作紀錄，不保存轉換後圖資料 |
| 原始來源 | SQLite `.db` | 被探勘與讀取的關聯式資料 |
| 語義層 | YAML + `SemanticMapper` | 宣告資料表如何成為圖譜的實體、屬性、關係 |
| 圖資料庫 | Neo4j | 保存各專案建出的節點、關係及全文索引 |
| AI 編排 | LangGraph、OpenAI Python SDK | 判斷聊天／查詢，工具呼叫、驗證、修正與執行 |

## 3. 專案中的主要目錄

```text
SogaVKG/
├─ frontend/                         # React 前端
│  └─ src/pages/                     # 專案、建置、問答、圖譜等畫面
├─ backend/
│  ├─ app/api/                       # FastAPI 路由：builder.py、query.py
│  ├─ app/semantic/                  # Mapping、Cypher builder、匯入管線
│  ├─ app/services/                  # Agent、查詢服務、工作服務、Mapping service
│  ├─ app/repositories/              # platform_state.db 的存取封裝
│  ├─ app/adapters/                  # Neo4j adapter 與資料庫介面
│  └─ data/platform_state.db          # 平台中繼資料
└─ semantic/
   ├─ mappings/                       # 舊／全域 Mapping（相容用途）
   └─ projects/<project_id>/mappings/ # 專案實際使用的 Mapping YAML
```

## 4. 前端資訊架構與畫面流程

前端入口是 `frontend/src/App.jsx`，使用 BrowserRouter。API 位址由 `frontend/src/config.js` 的 `apiUrl()` 統一組合；未設定 `VITE_API_BASE_URL` 時，Vite 開發伺服器會把 `/builder`、`/query`、`/health` 代理到 `127.0.0.1:8000`。

| 路徑 | 元件 | 使用者目的 | 主要 API |
|---|---|---|---|
| `/` | `ProjectsPage` | 列出、建立專案 | `GET/POST /builder/projects` |
| `/projects/:projectId` | `ProjectOverviewPage` | 看專案、資料來源、Mapping、建置摘要 | `GET /builder/projects/{id}/overview` |
| `/projects/:projectId/sources` | `SourcesPage` | 管理已儲存資料來源 | `/builder/sources` |
| `/projects/:projectId/mappings` | `MappingVersionsPage` | 看版本與回復 Mapping | `/builder/projects/{id}/mapping-versions` |
| `/projects/:projectId/build` | `BuildPage` | 探勘 schema、設定 Mapping、預覽、建置 | `/builder/source/schema`、`/mapping`、`/build` |
| `/projects/:projectId/graph` | `SemanticGraph` | 顯示 Neo4j 實際節點和關係 | `/query/graph/data` |
| `/projects/:projectId/ask` | `AskPage` | 用自然語言查詢該專案 | `POST /query/ask` |
| `/build`、`/ask` | 共用畫面 | 舊入口／未綁定專案的入口 | 同上，但建議使用專案內路由 |

### 4.1 使用者操作的主線（可作一張流程投影片）

```text
建立專案
  → 輸入 SQLite 檔案路徑
  → 探勘 Schema（表、欄位、外鍵、前五筆樣本）
  → 自動產生初始 Entity / Property / Relation Mapping
  → 人工修改語義名稱、主鍵、欄位、關係
  → 儲存 Mapping
  → 驗證 Mapping 與資料表是否吻合
  → 預覽會建立的節點數／關係匹配情形
  → 確認後執行全量重建或增量同步
  → 背景工作匯入 Neo4j，前端輪詢進度
  → 圖譜檢視、結構化查詢或 AI 自然語言查詢
```

## 5. 資料模型：三種資料不可混淆

| 資料類型 | 儲存位置 | 範例 | 用途 |
|---|---|---|---|
| 原始業務資料 | 使用者指定的 SQLite 檔 | `assets`、`locations` 資料表 | 建置的來源 |
| 平台中繼資料 | `platform_state.db` | 專案名稱、資料來源路徑、job 狀態、Mapping 版本 | 管理平台操作 |
| 知識圖譜資料 | Neo4j | `(:Assets)-[:HAS_LOCATIONS]->(:Locations)` | 被視覺化與查詢的結果 |

`platform_state.db` 的主要資料表：

| 表 | 重要欄位 | 說明 |
|---|---|---|
| `projects` | `id`, `name`, `description`, `created_at`, `updated_at` | 專案基本資料 |
| `source_profiles` | `id`, `project_id`, `name`, `source_type`, `config_json`, `active` | 可重複使用的來源設定；SQLite 路徑在 `config_json` |
| `mapping_versions` | `project_id`, `version`, `mappings_json` | 每次開始建置時保存 Mapping 快照 |
| `ingestion_jobs` | `id`, `project_id`, `status`, `logs_json`, `error`, `result_json` | 建置工作和進度紀錄 |

Neo4j 的每個節點皆會帶有：

| 欄位 | 意義 |
|---|---|
| `_source_id` | 原始表主鍵值，作為同一實體的合併識別 |
| `_project_id` | 專案 UUID；用於讀取與查詢時隔離不同專案 |
| `_inactive`、`_inactive_at` | 由停用 API 標記的狀態（目前查詢／視覺化是否排除它，需由後續產品規則明確定義） |

## 6. Mapping：從關聯式資料到圖譜的契約

Mapping 是系統最關鍵的設定。它描述「一個來源資料表」如何映射成「一種 Neo4j 節點」，以及外鍵如何映射成圖關係。

```yaml
project_id: <專案 UUID>
entity: Assets                    # 語義實體名稱；AI 與 API 都使用它
label: assets                     # 顯示名稱
source:
  node_label: Assets              # Neo4j 標籤
ingestion:
  source_type: sqlite             # 匯入讀取器名稱
  source_table: assets            # SQLite 資料表名稱
  primary_key: id                 # 來源唯一鍵，也成為 _source_id
properties:
  name:
    column: name                  # 來源欄位
    type: string                  # 目前供語義／UI 描述使用
    # fulltext: true              # 可選；建立 Neo4j 全文索引
relations:
  location:
    label: location_id
    relationship_type: HAS_LOCATIONS
    ingestion:
      foreign_key: location_id    # 本表的 FK 欄位
      target_key: id              # 對方資料表對應鍵；省略時採目標 entity 的 primary_key
    target:
      entity: Locations           # 必須是另一個 Mapping 的 entity
    display:
      property: name              # 查詢 relation 時回傳目標節點的哪一個屬性
```

### 6.1 Mapping 各區塊與參數

| 區塊／參數 | 必要性 | 說明與影響 |
|---|---|---|
| `project_id` | API 儲存時必要 | 決定 YAML 會保存至哪個 `semantic/projects/{id}/mappings/` 目錄 |
| `entity` | 必要 | 語義層的唯一名稱，例如 `Assets`；Agent 的 SemanticQuery 必須使用此值 |
| `label` | 建議 | UI 顯示的友善名稱，不改變 Neo4j label |
| `source.node_label` | 必要 | Neo4j 節點標籤；會被寫進 Cypher，應採安全、固定的命名 |
| `ingestion.source_type` | 必要 | 目前建置流程實際只註冊 `sqlite` reader |
| `ingestion.source_table` | 必要 | 原始 SQLite 表名；驗證與讀取均使用它 |
| `ingestion.primary_key` | 建議 | 原始資料唯一鍵；空值的資料列會略過，不建立節點 |
| `properties.<語義欄位>.column` | 必要 | 對應來源欄位；建置後其值成為 Neo4j 節點 property |
| `properties.<...>.type` | 建議 | `string`、`number`、`boolean` 等；目前主要作描述與 UI 推斷，資料仍以來源值為主 |
| `properties.<...>.fulltext` | 可選 | true 時在 Neo4j 建立對應 label 的全文索引，可讓 `LIKE` 走全文索引邏輯 |
| `relations.<名稱>.relationship_type` | 必要 | Neo4j 關係類型，例如 `HAS_CATEGORIES` |
| `relations.<...>.ingestion.foreign_key` | 必要 | 來源表指向目標表的外鍵欄位 |
| `relations.<...>.target.entity` | 必要 | 目標 Mapping 的 `entity`；驗證時會確認存在 |
| `relations.<...>.display.property` | 必要 | 取 relation 欄位時，回傳目標節點的這個 property |

### 6.2 Schema 探勘如何自動產生 Mapping

`POST /builder/source/schema` 會讀取 SQLite 的：資料表、`PRAGMA table_info`、`PRAGMA foreign_key_list`、每表前五筆樣本、資料列數。前端接著：

1. 將表名轉 PascalCase 作為 `entity` 與 `node_label`。
2. 以資料庫定義的 PK（沒有則預設 `id`）設成 `primary_key`。
3. 每個欄位產生 property；含 `INT`、`REAL`、`NUM`、`DECIMAL`、`FLOAT`、`DOUBLE` 的欄位推定為 `number`，`BOOL` 為 `boolean`，其餘為 `string`。
4. 每一個資料庫外鍵產生 relation；目標 entity 由目標表名轉 PascalCase，關係類型預設 `HAS_<目標表名>`。
5. 使用者應檢查自動推定結果，尤其是複合鍵、無資料庫 FK、非 `id` 主鍵與語義名稱。

## 7. 建置資料流程（Builder Pipeline）

### 7.1 前端到後端的完整時序

```text
BuildPage
  │ 選擇資料表與模式（full / incremental）
  │
  ├─ POST /builder/mapping（每個選取 entity）
  │     └─ MappingService 寫入專案 YAML
  ├─ POST /builder/projects/{projectId}/validate-mappings
  │     └─ 逐一檢查表、PK、property column、relation FK、目標 entity
  ├─ POST /builder/projects/{projectId}/build-preview
  │     └─ 統計節點數、空主鍵、外鍵可匹配／不可匹配數
  ├─ 使用者確認預覽
  └─ POST /builder/build
        ├─ 將當下 Mapping 存成 mapping_versions 快照
        ├─ 建立 ingestion_jobs 記錄（pending）
        ├─ FastAPI BackgroundTasks 執行 run_ingestion_job
        └─ 回傳 job_id；前端每 1.2 秒 GET /builder/build/{job_id}
```

### 7.2 後台工作狀態

`IngestionJob` 的狀態是 `pending`、`running`、`done`、`failed`。

1. API 建立 job 並立即回傳，避免瀏覽器等待大量資料匯入完成。
2. `run_ingestion_job()` 將 job 改為 `running`。
3. `IngestionPipeline` 每個步驟透過 `on_progress(step, message)` 寫入 `logs_json`。
4. 成功時將 `result_json` 寫回並標為 `done`；例外則保存 `error` 並標為 `failed`。
5. 前端以 job ID 輪詢並顯示 log、錯誤與統計。

### 7.3 IngestionPipeline 的內部動作

```text
SQLiteSourceReader.read(source_table)
  → 每個 Mapping 讀取來源表所有資料列
  → 建立／更新節點（MERGE _source_id + _project_id）
  → 逐一建立關係（MATCH 來源節點與目標節點，再 MERGE 關係）
  → 為 fulltext: true 的 property 建立 Neo4j 全文索引
  → 回傳 entities、relations、created、updated、unchanged 等摘要
```

節點寫入使用 `MERGE (n:Label { _source_id, _project_id })`，因此同專案、同實體、同來源 PK 的資料會更新而非重複建立。

### 7.4 全量重建與增量同步

| 模式 | 前端傳送 `mode` | 管線 `reset` | 現行行為 |
|---|---|---|---|
| 全量重建 | `full` | `true` | 僅刪除 `_project_id` 為目前專案的節點與關係，再完整匯入該專案資料 |
| 增量同步 | `incremental` | `false` | 以 `_source_id` + `_project_id` MERGE；統計新增、更新、未變更資料 |

全量重建以 `MATCH (n { _project_id: $project_id }) DETACH DELETE n` 限定刪除目前專案的資料；因此多個專案可以共用同一個 Neo4j instance 而不互相清除。沒有 `project_id` 的舊版單一專案 ingest endpoint，才保留清空所有資料的相容行為。

## 8. AI Agent／自然語言檢索：怎麼呼叫後方模組

Agent 實作在 `backend/app/services/nl_query_graph.py`，類別為 `NLQueryGraph`。它不是直接讓 LLM 產生並執行任意 Cypher；而是用 LangGraph 將模型限制為輸出一個受 Pydantic 驗證的 `SemanticQuery`，再由程式碼產生參數化 Cypher。

### 8.1 Agent 呼叫鏈（核心簡報圖）

```text
AskPage（使用者問題 + project_id）
  │ POST /query/ask
  ▼
query.py：get_nl_graph(project_id)
  ├─ 載入該專案 YAML → SemanticMapper
  ├─ 由 Mapping 產生 schema_text → build_schema_description()
  ├─ 建立 Neo4jAdapter / QueryService
  └─ 建立 NLQueryGraph
       │
       ▼ LangGraph StateGraph
       translate（LLM + function calling）
         ├─ 純聊天：直接 END，回傳文字
         └─ 呼叫 build_semantic_query
              ▼
           validate（SemanticQuery Pydantic + CypherQueryBuilder 預檢）
              ├─ 不合法：把錯誤帶回 translate，最多重試 2 次
              └─ 合法
                   ▼
                execute（QueryService）
                   ▼
                CypherQueryBuilder → 參數化 Cypher + params
                   ▼
                Neo4jAdapter.execute()
                   ▼
                AskPage 顯示結果表格與 translated_query
```

### 8.2 為何稱為 Agent

它具備「依情境選擇工具、驗證工具輸出、失敗後修正並再嘗試」的 Agent 行為：

| Agent 節點 | 做什麼 | 呼叫的後方模組 |
|---|---|---|
| `translate` | 把問題與可用 schema 送至 LLM；模型決定聊天或呼叫工具 | OpenAI-compatible `chat.completions.create()` |
| `validate` | 驗證工具參數、實體／欄位／關係是否存在 | `SemanticQuery`、`CypherQueryBuilder.build()` |
| `execute` | 執行已驗證查詢 | `QueryService.execute()` → `Neo4jAdapter.execute()` |
| conditional route | 成功執行、重試翻譯或終止 | LangGraph `StateGraph` |

目前重試上限 `MAX_RETRIES = 2`。例如模型使用不存在的欄位，Cypher builder 會回 `Unknown field: Entity.field`；此訊息加入下一次 prompt，要求模型依現有 schema 修正。兩次仍失敗才回傳錯誤給使用者。

### 8.3 LLM 可呼叫的工具：`build_semantic_query`

這是唯一暴露給模型的 function-calling 工具。模型不取得資料庫密碼、不直接取得 Cypher 執行權，也不能任意指定資料表。

| 參數 | 型別 | 必要 | 意義 |
|---|---|---|---|
| `entity` | string | 是 | Mapping 裡存在的實體，如 `Assets` |
| `fields` | string[] | 否 | 要回傳的語義欄位；空白時 Query Builder 取該 entity 所有 properties |
| `filters` | object[] | 否 | 篩選條件陣列 |
| `filters[].field` | string | 是（每個 filter） | property 或 relation 名稱，禁止自己發明名稱 |
| `filters[].operator` | enum | 是 | `=`, `!=`, `>`, `>=`, `<`, `<=`, `LIKE`, `IN` |
| `filters[].value` | any | 是 | 比對值；`IN` 必須是陣列；`LIKE` 填關鍵字本身，不需要 `%` |
| `order_by.field` | string | 否 | 排序欄位 |
| `order_by.direction` | `ASC` / `DESC` | 否 | 排序方向，預設 `ASC` |
| `limit` | integer | 否 | 筆數上限；Pydantic 限制 1–500，預設 20 |

範例：使用者問「列出 C217 地點的資產名稱與數量」。模型應呼叫：

```json
{
  "entity": "Assets",
  "fields": ["name", "quantity", "location"],
  "filters": [
    {"field": "location", "operator": "LIKE", "value": "C217"}
  ],
  "limit": 20
}
```

### 8.4 SemanticQuery 參數化與安全性

`SemanticQuery` 是 Pydantic model，定義在 `backend/app/schemas/semantic_query.py`。`CypherQueryBuilder` 只從 Mapping 解析 entity、property、relation；值則以 Neo4j 參數傳遞。

對上述範例，概念上會產生：

```cypher
MATCH (n:Assets), (n)-[:HAS_LOCATIONS]->(n_location:Locations)
WHERE n._project_id = $project_id
  AND n_location.name CONTAINS $filter_0
RETURN n.name AS name, n.quantity AS quantity, n_location.name AS location
LIMIT 20
```

```json
{"project_id": "<project UUID>", "filter_0": "C217"}
```

這個設計的防護重點：

1. 值不以字串拼接，而採 `$filter_0` 等參數，降低注入風險。
2. entity 與欄位必須由 YAML Mapping 白名單解析；未知欄位會被拒絕。
3. relation 的目標 node label 與關係 type 由 Mapping 決定，非模型自由輸入。
4. 每個查詢會加上主節點的 `_project_id` 條件，避免讀到其他專案的主節點資料。

### 8.5 Agent 需要的環境參數

| 環境變數 | 用途 | 是否必要 |
|---|---|---|
| `MY_MODEL_BASE_URL` | OpenAI-compatible 模型服務根網址 | AI 問答必要 |
| `MY_MODEL_NAME` | 要呼叫的模型名稱 | AI 問答必要 |
| `MY_MODEL_API_KEY` | 模型 API 金鑰；程式提供 `not-needed` 預設給某些自架服務 | 視模型服務而定 |
| `NEO4J_URI` | Neo4j 連線位址 | 圖譜查詢／建置必要 |
| `NEO4J_USER` | Neo4j 帳號 | 圖譜查詢／建置必要 |
| `NEO4J_PASSWORD` | Neo4j 密碼 | 圖譜查詢／建置必要 |

## 9. API 參考

所有 Builder API 以 `/builder` 開頭，Query API 以 `/query` 開頭。FastAPI 會將 Pydantic 驗證錯誤回傳為 422；常見的 400 是使用者輸入或設定不合法，404 是資源不存在，503 通常代表 Neo4j／模型環境變數未設定。

### 9.1 專案與資料來源

| 方法與路徑 | 請求參數／body | 成功回傳 | 用途 |
|---|---|---|---|
| `GET /builder/projects` | 無 | `{projects: [...]}` | 列出專案 |
| `POST /builder/projects` | `{name, description?}` | 新專案物件 | 建立專案；`name` 1–120 字，`description` 最長 500 字 |
| `GET /builder/projects/{project_id}/overview` | path: `project_id` | 專案、來源數、實體數、關係數、版本、builds | 專案首頁摘要 |
| `GET /builder/sources?project_id=` | query: 可選專案 ID | `{sources: [...]}` | 列出來源設定 |
| `POST /builder/sources` | `{project_id?, name, source_type, config}` | source 物件 | 保存資料來源；目前 `source_type` 必須是 `sqlite`，`config.path` 是後端可讀的檔案路徑 |
| `DELETE /builder/sources/{source_id}` | path: `source_id` | `{status, id}` | 刪除來源設定 |
| `POST /builder/sources/{source_id}/active?active=` | query: boolean | 更新後 source | 啟用／停用來源 |
| `POST /builder/source/schema` | `{source_type: "sqlite", path}` | tables、columns、foreign_keys、sample_rows | 探勘 SQLite schema |

來源建立範例：

```json
{
  "project_id": "06b7293f-2847-45c3-a374-4230cd547018",
  "name": "實驗室資產 SQLite",
  "source_type": "sqlite",
  "config": {"path": "/absolute/path/to/lab.db"}
}
```

### 9.2 Mapping、驗證、預覽與建置

| 方法與路徑 | body／參數 | 成功回傳 | 用途 |
|---|---|---|---|
| `POST /builder/mapping` | 完整 Mapping JSON | `{status: "saved", entity, file}` | 寫入該專案 YAML |
| `GET /builder/mappings?project_id=` | query: 專案 ID | `{count, mappings}` | 讀取目前 draft Mapping |
| `GET /builder/mapping/{entity}?project_id=` | path/query | Mapping JSON | 讀單一 Mapping |
| `POST /builder/projects/{id}/validate-mappings` | `BuildRequest` | `{valid, errors, warnings}` | 驗證 Mapping 與資料庫 schema |
| `POST /builder/projects/{id}/build-preview` | `BuildRequest` | 每個 entity 的列數、節點數、關係匹配 | 先評估建置品質 |
| `POST /builder/build` | `BuildRequest` | `{job_id, status, mapping_version}` | 建立非同步建置工作 |
| `GET /builder/build/{job_id}` | path: job ID | job status、logs、error、result | 輪詢建置進度 |
| `GET /builder/projects/{id}/mapping-versions` | path: project ID | `{versions}` | 版本列表 |
| `GET /builder/projects/{id}/mapping-versions/{version}` | path | Mapping snapshot | 讀特定版本 |
| `POST /builder/projects/{id}/mapping-versions/{version}/restore` | path | 回復結果 | 將歷史版本覆蓋成目前 draft |

`BuildRequest`：

```json
{
  "reset": true,
  "mode": "full",
  "source_path": "/absolute/path/to/lab.db",
  "source_id": "已保存的 source profile UUID",
  "project_id": "專案 UUID"
}
```

| 參數 | 型別／預設 | 說明 |
|---|---|---|
| `reset` | boolean，預設 `true` | 目前 `/builder/build` 實際以 `mode == "full"` 決定重設，`reset` 是相容欄位 |
| `mode` | string，預設 `full` | `full` 為全量重建，`incremental` 為增量同步 |
| `source_path` | string/null | 未選 `source_id` 時使用的 SQLite 路徑 |
| `source_id` | UUID/null | 優先使用保存的來源設定；停用來源會拒絕建置 |
| `project_id` | UUID/null | `/builder/build` 必填；決定 Mapping、Neo4j 資料隔離與 job 歸屬 |

### 9.3 圖譜與查詢 API

| 方法與路徑 | 參數／body | 回傳 | 用途 |
|---|---|---|---|
| `POST /query/?project_id=` | `SemanticQuery` | `{data, count}` | 不透過 LLM 的結構化查詢 |
| `POST /query/ask` | `{question, project_id}` | chat 或 query 結果 | AI Agent 問答 |
| `GET /query/graph?project_id=` | query: project ID | schema nodes、edges | 讀語義圖譜結構（Mapping 層） |
| `GET /query/graph/stats?project_id=` | query: project ID | `{nodes, edges}` | Neo4j 實際資料統計 |
| `GET /query/graph/data?project_id=&entity?&limit=` | `limit` 1–500 | 視覺化節點、邊與數量 | 讀 Neo4j 資料供圖譜畫面 |
| `POST /query/graph/deactivate` | `{project_id, entity, source_ids}` | `{status, count}` | 標記來源 ID 對應節點為 inactive |

`SemanticQuery` 範例：

```json
{
  "entity": "Assets",
  "fields": ["name", "quantity", "category"],
  "filters": [{"field": "quantity", "operator": ">", "value": 0}],
  "order_by": {"field": "name", "direction": "ASC"},
  "limit": 20
}
```

`POST /query/ask` 的三類可能結果：

```json
{"success": true, "type": "chat", "reply": "您好，我可以協助查詢..."}
```

```json
{
  "success": true,
  "type": "query",
  "translated_query": {"entity": "Assets", "fields": ["name"], "filters": [], "limit": 20},
  "result": {"data": [{"name": "..."}], "count": 1},
  "attempts": 1
}
```

```json
{"success": false, "type": "query", "error": "Unknown field: Assets.foo", "attempts": 2}
```

## 10. 啟動與設定

後端 `.env` 不應提交，也不能放入前端 bundle。可從 `backend/.env.example` 建立。

```bash
cd backend
python3 -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

```bash
cd frontend
npm install
npm run dev
```

| 設定 | 放置位置 | 說明 |
|---|---|---|
| `VITE_API_BASE_URL` | `frontend/.env.local` | 前後端不同網域時填 API base URL；本機 Vite 開發可留空 |
| `CORS_ALLOW_ORIGINS` | `backend/.env` | 逗號分隔的前端 Origin；例如 `http://localhost:5173` |
| `SQLITE_DB_PATH` | `backend/.env` | 選用的預設 SQLite 路徑；Builder 通常由前端送 `source_path` 或 `source_id` |
| Neo4j 與模型環境變數 | `backend/.env` | 見第 8.5 節；絕不可放到 Git 或前端 |

## 11. 已實作範圍與目前限制

| 主題 | 現況 |
|---|---|
| SQLite | 已實作並可探勘、保存 profile、匯入 |
| Google Sheets、MySQL、PostgreSQL | 目前有 adapter 介面／檔案或規劃痕跡，但 Builder 實際拒絕非 SQLite；簡報不可宣稱已支援 |
| 多專案資料隔離 | 節點、查詢與全量重建皆以 `_project_id` 篩選；可在同一 Neo4j instance 保存不同專案 |
| Mapping 版本 | 每次 build 建立 snapshot，可讀取與回復 |
| 背景工作 | FastAPI `BackgroundTasks` + SQLite job 紀錄；適合單機／開發，不是可水平擴展的工作佇列 |
| Agent 記憶 | 目前每次 `/query/ask` 是獨立問題，沒有多輪對話歷史或使用者身分記憶 |
| 權限、登入、稽核 | 尚未實作；不可直接當多租戶正式系統使用 |
| Mapping 刪除 API | `DELETE /builder/mapping/{entity}` 目前走全域 `mapping_service`，未帶 project_id；多專案前應修正為專案 scoped |

## 12. 簡報建議頁序

1. **問題與定位**：關聯式資料難以以語義方式探索；Soga 將其變成可管理、可查詢的知識圖譜。
2. **系統架構**：使用第 2 節架構圖，強調 React、FastAPI、SQLite、YAML、Neo4j、LLM 的分工。
3. **使用者旅程**：使用第 4.1 節的建立到問答流程。
4. **Mapping 的價值**：用第 6 節 YAML 對照來源表、圖節點與關係。
5. **建置資料管線**：使用第 7.1 節時序圖與全量／增量比較。
6. **AI Agent 架構**：使用第 8.1 節，凸顯 LLM 不直接執行 Cypher。
7. **安全與可靠性**：Mapping 白名單、Pydantic 驗證、參數化 Cypher、兩次修正重試、project ID 隔離。
8. **Demo 情境**：SQLite 資產表 → Mapping → Build → 圖譜 → 問「C217 有哪些資產？」。
9. **已知限制與下一步**：誠實說明第 11 節，並規劃專案 scoped full reset、工作佇列、權限、更多 adapter。

## 13. 名詞速查

| 名詞 | 白話說明 |
|---|---|
| Entity | 圖譜中的一類事物，例如資產、地點、類別 |
| Property | Entity 的欄位，例如資產名稱、數量 |
| Relation | Entity 與 Entity 的連線，例如資產位於某地點 |
| Mapping | 從資料表欄位到圖譜 Entity／Property／Relation 的規格 |
| SemanticQuery | 與資料庫無關的語義查詢物件；由 Agent 或 API 產生 |
| Cypher | Neo4j 的圖資料庫查詢語言 |
| Source profile | 保存來源類型、名稱與連線／檔案路徑的設定 |
| Full rebuild | 清除既有圖資料後，完整重建 |
| Incremental sync | 以來源主鍵比對，新增或更新已變動資料 |
| Agent tool calling | LLM 不只回文字，而是依工具 schema 呼叫受控制的程式功能 |
